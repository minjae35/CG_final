# Activate conda env cg_final before: pip install -r requirements.txt
numpy>=1.24
scipy>=1.10
pillow>=10.0
pyyaml>=6.0
pydantic>=2.0
typer>=0.9.0
rich>=13.0
plyfile>=1.0  # PLY I/O (pipeline, placement, 3DGS merge)
trimesh>=4.0
pygltflib>=1.16.0
scikit-image>=0.22
scikit-learn>=1.3.0
lpips>=0.1.4
pycolmap>=0.4.0; python_version>="3.9"

# nvdiffrast: plain `pip install -r` uses build isolation → torch invisible → build fails.
# Install torch in this env first, then: pip install -r requirements.txt --no-build-isolation
# (optional) pip install ninja setuptools wheel
nvdiffrast @ git+https://github.com/NVlabs/nvdiffrast.git
